const first = 'Abid'
const last = 'Navid'

// const last1 = first + ' ' + last
// console.log(last1)

const last1 = first.concat(' ').concat(last)
console.log(last1)

//includes

console.log(last.includes('x'))